#ifndef ENUMS_H
#define ENUMS_H


enum class dis_type {
    home = 0,
    city = 1,
    wild = 2,
    rubb = 3
};

enum class run_type {
    home = 0,
    base = 1,
    no   = 2
};

enum class wuff_type {
    base = 0,
    wild = 1,
    rubb = 2
};


#endif // ENUMS_H
